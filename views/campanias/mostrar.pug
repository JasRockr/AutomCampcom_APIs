extends ../layout/app

block contenido

  //- pre= JSON.stringify(campaign, undefined, 2)

  div(class='py-5')
    h1(class="text-4xl my-3 font-extrabold text-center")= campaign.campania

    div 
      a(href="#" class="font-bold text-gray-600 text-5m") Categoria: 
          span(class="font-normal")= campaign.Categoria.nombre

    div(class="mt-5 md:flex md:gap-4 md:items-start")
          
        div(class="md:w-2/3 bg-white shadow rounded-lg py-8 px-4 mx-auto max-w-4xl md:px-10")
        
          h3(class="text-center pb-5 leading-6 text-2xl font-bold text-gray-900") Parametros de envío

          include ../templates/paramConsumo

        div(class="md:w-1/3 bg-white shadow rounded-lg") 

          div(class="px-5 py-10 space-y-5")
            h2(class="text-2xl leading-6 font-bold text-gray-900") Regla de Negocio

            p= campaign.regla

            a(href=`#` class="bg-green-600 w-full text-center block font-bold text-white p-2 uppercase rounded") Enviar SMS