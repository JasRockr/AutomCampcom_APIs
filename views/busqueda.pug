extends ./layout/app

block contenido

  //- pre= JSON.stringify(campaign, undefined, 2)

  section(class="py-5")
    h2(class="text-center text-2xl font-extrabold pt-10")= pagina

    if campanias.length
      div(class="grid grid-cols-1 lg:grid-cols-2 gap-5 mt-5")
        each campania in campanias
          include ./templates/card

    else
      p(class="text-center text-gray-800 text-sm py-10") No hay coincidencias para la Búsqueda
